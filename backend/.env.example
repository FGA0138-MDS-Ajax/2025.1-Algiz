# Configurações do Banco de Dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=econetdb

# Configurações de Administração
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
JWT_SECRET=projetoalgiz

# Configurações Opcionais (remova ou comente se não for usar)
# RESEND_API_KEY=the-api-key-from-resend  # Obrigatório para envio de emails
# RECAPTCHA_SECRET_KEY=the-secret-key-from-recaptcha  # Obrigatório para verificação reCAPTCHA

# Modo de operação quando serviços opcionais estão desativados
# FALLBACK_MODE=development  # Descomente para permitir fallbacks em desenvolvimento